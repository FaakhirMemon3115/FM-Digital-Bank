{
  "name": "fm-digital-bank",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix web",
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix web\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
